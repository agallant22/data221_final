import pandas
from sklearn.model_selection import train_test_split
# load the cleaned data set
dataset = pandas.read_csv("cleaned_car_details_dataset.csv")


features_train, features_test, labels_train, labels_test =train_test_split(feature_matrix, target_labels, test_size=0.3,random_state=42)
